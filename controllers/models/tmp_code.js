                //console.log("> tmp_root_item.craft_pnt " + tmp_root_item.craft_pnt);
                //console.log("> toCraft_array[i].quantity " + toCraft_array[i].quantity);



                //tmp_zaino_remaining = 0;
                // if (zaino) {
                //     for (let j = 0; j < zaino.length; j++) {
                //         if (zaino[j].item_id == tmp_root_item.id) {
                //             temp_zaino_quantity_dif = (zaino[j].item_quantity - toCraft_array[i].quantity);
                //             if (temp_zaino_quantity_dif < 0){
                //                 tmp_zaino_used = toCraft_array[i].quantity - zaino[j].item_quantity;
                //             } else{
                //                 tmp_zaino_used = toCraft_array[i].quantity;
                //             }
                //             console.log("> Trovato! ne ha " + zaino[j].item_quantity + " copie! Ne resteranno: " + temp_zaino_quantity_dif);
                //             already_avaible.push({ item_name: tmp_root_item.name, remaning_quantity: temp_zaino_quantity_dif, used_quantity: tmp_zaino_used});

                //             toCraft_array[i].quantity = Math.max(tmp_zaino_used, 0);
                //             break;
                //         }
                //     }
                // }

                //console.log("> ID: " + toCraft_array[i].id + " copie da craftare: " + toCraft_array[i].quantity);